# Django Settings
DJANGO_SETTINGS_MODULE=config.settings.development
DJANGO_SECRET_KEY=django-insecure-dev-key-change-in-production-abc123xyz

# Debug
DEBUG=True

# Database - PostgreSQL
# QUAN TRỌNG: Cập nhật DB_PASSWORD với password PostgreSQL của bạn
DATABASE_URL=postgres://postgres:1123@localhost:5432/englishstudy
DB_NAME=englishstudy
DB_USER=postgres
DB_PASSWORD=1123
DB_HOST=localhost
DB_PORT=5432

# Redis (optional for development)
REDIS_URL=redis://localhost:6379/0

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:8000

# JWT
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=60
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7

# Email (console backend for development)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Frontend URL (for OAuth redirects)
FRONTEND_URL=http://localhost:3000

# =============================================================================
# SOCIAL AUTH - Google OAuth2
# =============================================================================
# Hướng dẫn lấy credentials:
# 1. Truy cập https://console.cloud.google.com/
# 2. Tạo project mới hoặc chọn project có sẵn
# 3. APIs & Services > Credentials > Create Credentials > OAuth 2.0 Client ID
# 4. Application type: Web application
# 5. Authorized JavaScript origins: http://localhost:3000
# 6. Authorized redirect URIs: http://localhost:3000/login.html
# 7. Sao chép Client ID và Client Secret vào đây
GOOGLE_OAUTH2_CLIENT_ID=your-google-client-id-here
GOOGLE_OAUTH2_CLIENT_SECRET=your-google-client-secret-here

# =============================================================================
# SOCIAL AUTH - Facebook OAuth2
# =============================================================================
# Hướng dẫn lấy credentials:
# 1. Truy cập https://developers.facebook.com/
# 2. My Apps > Create App > Consumer > Đặt tên app
# 3. Add Products > Facebook Login > Set Up
# 4. Settings > Basic > Sao chép App ID và App Secret
# 5. Facebook Login > Settings:
#    - Valid OAuth Redirect URIs: http://localhost:3000/login.html
# 6. Make sure app is in Development mode for testing
FACEBOOK_APP_ID=your-facebook-app-id-here
FACEBOOK_APP_SECRET=your-facebook-app-secret-here
